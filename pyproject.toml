[project]
name = "shipline"
version = "0.1.0"
description = "Opinionated development lifecycle for Claude Code"
requires-python = ">=3.10"
license = {text = "MIT"}

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["scripts"]
